generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = "file:../notes-db.sqlite3"
}

model notes {
    title    String
    line     String
    filename String
    created  String
    updated  String

    @@id([title, line, filename])
}
