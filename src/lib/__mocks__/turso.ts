export const turso = {
    batch: jest.fn(),
    close: jest.fn(),
    closed: false,
    execute: jest.fn(),
    executeMultiple: jest.fn(),
    protocol: '',
    sync: jest.fn(),
    transaction: jest.fn(),
};
