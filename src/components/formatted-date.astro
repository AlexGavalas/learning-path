---
import type { HTMLAttributes } from 'astro/types';
import { format as formatDate } from '@formkit/tempo';

interface Props extends HTMLAttributes<'time'> {
    dateString: string;
    format?: string;
    timezone?: string;
}

const {
    dateString,
    format = 'D MMM YYYY',
    timezone = 'Europe/Athens',
    ...rest
} = Astro.props;

const datetime = formatDate({
    date: dateString,
    format: 'YYYY-MM-DD',
    tz: timezone,
});

const formattedDate = formatDate({
    date: dateString,
    format,
    tz: timezone,
});
---

<time datetime={datetime} {...rest}>{formattedDate}</time>
